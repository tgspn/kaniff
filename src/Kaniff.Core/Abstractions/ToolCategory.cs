namespace Kaniff.Core.Abstractions;

/// <summary>High-level grouping used by the CLI and desktop UI to organize tools.</summary>
public enum ToolCategory
{
    Encoding,
    Network,
    Security,
    Text,
    Json
}
